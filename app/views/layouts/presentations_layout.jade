!!! 5
html
  head
    title= title
    != stylesheetLinkTag('bootstrap', 'bootstrap-responsive', 'application')
    != javascriptIncludeTag('https://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js', 'rails', 'bootstrap', 'underscore', 'backbone/backbone', 'models', 'views',  'backbone-app' )
    != csrfMetaTag()
  body
    .navbar.navbar-inverse
        .navbar-inner
            .container
               a.brand(href='#') Markup Presentation

    .container
      - var flash = request.flash('info').pop(); if (flash) {
        .alert.alert-info= flash
      - }

      - flash = request.flash('error').pop(); if (flash) {
        .alert.alert-error= flash
      - }

      != body

      hr
      footer
        p @ UI Bootcamp - III (2013)
  != contentFor('javascripts')
